Run a code cleanup pass on this project:
-Remove unused CSS rules and selectors (check that each is used in at least one HTML file).
-Consolidate duplicate or redundant styles (same properties, repeated blocks) into shared rules or variables.
-Remove dead or commented-out code in HTML, CSS, and JS unless it’s clearly intentional (e.g. “TODO” or “deprecated” notes).
-Improve organization of code where applicable without removing any of the website's existing styles, patterns, or functionalities.
-Update html components in the components folder to be consistent with what is actually used in the website.
-Update design-system.html to include all the latest styles and patterns and remove any unused styles or patterns.
-Show a short summary of what you changed.
